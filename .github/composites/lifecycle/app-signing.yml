# Configuration for Signing of app
# `💡` Lifecycle folder contains composite workflows that are involved with the CI\CD lifecycle of chapchap such as signing of application code before deployment.

name: "Code sign android Application"
description: "Creates a github release the various builds of the app"
runs:
  using: "composite"
  steps:
    - name: Sign APK
      if: startsWith(github.ref, 'refs/tags/') && github.repository == 'WalterBanda/chapchap'
      uses: r0adkll/sign-android-release@v1
      with:
          releaseDirectory: build/app/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

    - name: Sign AAB 
      if: startsWith(github.ref, 'refs/tags/') && github.repository == 'WalterBanda/chapchap'
      uses: r0adkll/sign-android-release@v1
      with:
          releaseDirectory: build/app/outputs/bundle/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
