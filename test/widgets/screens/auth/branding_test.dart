import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Splash screen tests', () {
    testWidgets('Test if splash screen show the respective UI element',
        (widgetTester) {});
    testWidgets('Test splash screen element dimensions', (widgetTester) {});
  });
}
